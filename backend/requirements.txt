fastapi==0.136.1
# Starlette 1.0.0 (the first stable release) bundles every CVE patch the
# old 0.52.1 floor was carrying — CVE-2025-54121 + CVE-2025-62727 plus
# everything from the 0.5x branch. FastAPI 0.136.1's bound is now
# `starlette>=0.46.0` (open-ended on the upper side), so no manual
# narrowing is needed.
starlette==1.0.0
uvicorn[standard]==0.47.0
httpx==0.28.1
mutagen==1.47.0
playwright==1.60.0