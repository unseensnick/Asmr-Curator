[project]
name = "asmr-filename-generator"
version = "1.0.0"
description = "Self-hosted ASMR filename generator with OCR and tag dictionary"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.115.5",
    "uvicorn[standard]==0.32.1",
    "python-multipart==0.0.12",
]
